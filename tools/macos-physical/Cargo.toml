[package]
name = "nbreq-macos-physical"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
nbreq = { path = "../.." }

[workspace]

[lints.rust]
unsafe_code = "forbid"
warnings = "deny"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
unwrap_used = "deny"
