[package]
name = "nbreq"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
description = "Runtime-independent cancellable HTTP client"
publish = false

[features]
default = []
curl-pilot = ["dep:curl"]
curl-pilot-vendored = ["curl-pilot", "curl/ssl"]
native = ["dep:httparse", "dep:mio"]
test-support = []
ffi = []

[dependencies]
curl = { version = "=0.4.50", optional = true, default-features = false, features = ["poll_7_68_0", "nbreq-explicit-init"] }
httparse = { version = "=1.10.1", optional = true }
mio = { version = "=1.0.4", optional = true, features = ["net", "os-poll"] }

[dev-dependencies]
rcgen = "=0.13.2"
rustls = { version = "=0.23.42", default-features = false, features = ["ring", "std", "tls12"] }
socket2 = "=0.6.5"

[patch.crates-io]
curl = { path = "vendor/curl" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
unwrap_used = "deny"
